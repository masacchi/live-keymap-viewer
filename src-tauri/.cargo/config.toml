# Windows向けのクロスビルド(cargo-xwin)で、MSVCのCRTのデバッグ情報(.pdb)が無いという
# 警告(LNK4099)がライブラリの数だけ出て、ほかの警告が読めなくなる。.pdbはxwinが落としてこない
# もので、exeの動作には関係しないので黙らせる
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-arg=/ignore:4099"]
